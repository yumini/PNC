{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
<p>&nbsp;</p>

 <div class="row">
            <div class="col-lg-3">
            </div>
            <div class="col-lg-6" style="text-align: center">
      <div class="well">
          <p><h3>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</h3></p>
    {% if app.session is not empty %}
        {% set targetUrl = app.session.get('_security.' ~ app.security.token.providerKey ~ '.target_path') %}
        {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
    {% endif %}
        Tu petición de registro ha sido enviada satisfactoriamente. Le enviaremos una notificación a su cuenta de email registrada en cuanto confirmemos sus datos registrados</div>
               
            </div>
            <div class="col-lg-3">
            </div>
        </div>

{% endblock fos_user_content %}
